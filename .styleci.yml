preset: laravel

risky: false

disabled:
  - concat_without_spaces
  - alpha_ordered_imports

finder:
  exclude:
    - modules
    - node_modules
    - storage
    - vendor
  name: "*.php"
  not-name: "*.blade.php"
